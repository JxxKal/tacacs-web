name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  backend-lint-test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip
      # - working-directory: backend
      #   run: |
      #     pip install -e ".[dev]"
      #     ruff check .
      #     mypy app
      #     pytest -q
      - name: placeholder (M1 — fill in once backend lands)
        run: echo "TODO: backend lint+test"

  frontend-lint-test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      # - working-directory: frontend
      #   run: |
      #     npm ci
      #     npm run lint
      #     npm run type-check
      #     npm test -- --run
      #     npm run build
      - name: placeholder (M1 — fill in once frontend lands)
        run: echo "TODO: frontend lint+test+build"
